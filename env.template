# Database Configuration Template
# Copy this file to .env.local and fill in your actual values
# 
# IMPORTANT: .env.local is gitignored and will not be committed to version control

# Database Host (required)
# Examples:
#   - localhost
#   - your-db-host.com
#   - your-db-host.com:5432 (if port is included)
DB_HOST=localhost

# Database Port (optional, defaults to 5432)
# Only needed if port is not included in DB_HOST
DB_PORT=5432

# Database Name (required)
DB_NAME=your_database_name

# Database User (required)
DB_USER=your_database_user

# Database Password (required)
DB_PASS=your_database_password

# Table Name (optional, defaults to "bms_data")
# Can be schema-qualified: "public.iotbms_telemetry"
# Or simple: "bms_data"
TABLE_NAME=bms_data

